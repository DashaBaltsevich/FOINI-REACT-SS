



export const RegForm = () => {

    return (
        <form onSubmit={}>
            <label>
                Имя:
                <input type="text" value={firstNameValue} onChange={} />
            </label>
        </form>
    );
};